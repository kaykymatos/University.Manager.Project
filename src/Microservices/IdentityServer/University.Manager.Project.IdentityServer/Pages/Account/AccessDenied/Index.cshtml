@page 
@model University.Manager.Project.IdentityServer.Pages.Account.AccessDenied.Index
@{
}
<div class="row">
    <div class="col">
        <h1>Access Denied</h1>
        <p>You do not have permission to access that resource.</p>
        <div class="mt-3">
            <form asp-page="/Account/AccessDenied/Index">
                <input type="hidden" asp-for="Input.ReturnUrl" value="@HttpContext.Request.Query["returnUrl"]" />
                <button class="btn btn-primary" name="Input.Button" value="back">Back</button>
            </form>
        </div>
    </div>
</div>